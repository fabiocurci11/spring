
spring:
    application:
        name: dev-faby-BE

    datasource:
        url: jdbc:mysql://mysql-3edb651-fabio-db.j.aivencloud.com:24158/faby-db
        username: avnadmin
        password: ${DB_PASSWORD}

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
